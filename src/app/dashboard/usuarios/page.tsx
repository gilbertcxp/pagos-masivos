import Placeholder from "@/components/Placeholder";

export default function Page() {
  return (
    <Placeholder
      titulo="Usuarios"
      descripcion="Administrar usuarios y roles (Administrador / Usuario)."
    />
  );
}
